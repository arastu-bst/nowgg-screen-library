// Shared chrome for the header brand pills (BluestacksCta · NowPrimeCta): a white-10 glass
// pill with the now.gg 0.8px hairline + soft drop shadow. Each consumer adds its own
// gap / py / content. Extracted (Gate 3 at 2 consumers) so the pill look can't drift.
export const HEADER_PILL =
  'inline-flex shrink-0 items-center rounded-pill border-hair border-white-20 bg-white-10 px-3 shadow-pill transition-colors hover:bg-white-20'
