# now.gg — Scratchpad
Inline correction-resolution log. One line per correction the moment it's
resolved. Promoted to decisions.md / taste.md at audit passes, then wiped.

Format: `YYYY-MM-DD HH:mm — <file> — <what changed> — Why: <one phrase>`

--- Pending audit entries ---
2026-06-11 — ProfileSidebar.tsx (NotchButton help) — swapped hand-drawn bare "?" path → library circled-? glyph `Icon name="help" size={16}` — Why: taste-9 (don't hand-draw a glyph the library has); now.gg help affordance is the circled-? everywhere, play-page help uses the library glyph (Gate 2.2 sibling). Chose 16/help.svg (full-opacity) over 24/help-icon.svg (baked stroke-opacity 0.8) so the masked glyph can render pure white. Recurring authoritative-source category.
2026-06-11 — ProfileSidebar.tsx (both notch glyphs) — bumped size-4 (16px) → size-5 (20px) — Why: 16px in a 44px button = 36% fill, read undersized; footer's identical 44px social buttons use 20px (Gate 7 consistency / Gate 8 fill).
2026-06-11 — ProfileSidebar.tsx (NotchButton) — glyph color text-text-secondary (80% white) → text-text-primary (pure white); dropped redundant hover:text-text-primary — Why: designer wants white. Notch sits OUTSIDE the panel on the black-70 scrim → needs full presence; footer social glyphs stay secondary (inside glass on dark chips, different context). Flag at audit: confirm notch-vs-footer color split is intentional.
2026-06-11 — ProfileSidebar.tsx (header) — removed the desktop avatar img from the header; header is now title-only — Why: the avatar already renders in the profile row immediately below → duplicate signal (Gate 8 duplicate-signal miss from the S5 build). Designer caught. Desktop close = notch button; mobile close = the X in header (kept). Generalization candidate: a drawer/panel header shouldn't repeat an identity element shown in its body.
2026-06-11 — ProfileSidebar.tsx (stat cards) — rebuilt to Figma "User Profile / Stats Row" spec (nodes 26502:58932 Stat1 / 26502:59014 Stat2, file qmq3nfiuJyFZ3feVIMfCQh, read via Desktop Bridge). Compact h-11 (44px), border-line(white-10)→border-white-20, p-4→px-2.5, label text-2xs/muted→text-3xs(10px)/text-tertiary(70%, Figma "Text/Body") leading-3 tracking-[0.02em] (Figma ls 2% — exact one-off), value text-base/bold→text-2xs(12px)/semibold leading-4, gap-3→gap-2 (Figma row gap 8), labels→Title Case. — Why: designer flagged not-Figma-accurate. Authoritative-source: pulled the real node specs+tokens from Figma instead of guessing.
2026-06-11 — public/profile/badge-game-cards.svg + badge-gold-cauldron.svg (NEW assets) — exported the real Figma "Illustration" frames (frosted white-10 tile + full-color badge) as SVG via figma_execute exportAsync; rendered absolute inset-y-0 right-0 h-full in each card, content pr-10 (Figma Content right-pad 40) to clear them. — Why: designer said "use the assets from figma"; the badges are multi-color illustrations (game-cards / gold-cauldron), NOT maskable monochrome icons, so they live in public/profile/ not the currentColor icon library. NOTE: the OLD cards rendered NO icon — Icon name="gamepad"/"gameplay-time" 404'd (never in the 419-icon library); a broken/invisible asset shipped at S5 (Gate 8 verify-it-renders miss). Generalization candidate: monochrome→Icon mask; multi-color illustration→exported SVG asset.
2026-06-11 — ProfileSidebar.tsx (profile-row edit) — replaced Icon name="edit" (library edit.svg = a SOLID PINK pencil, masked to 70% grey) with the inline Figma "Edit Details" pencil (node 26502:62323): white thin/outlined pencil, currentColor, size-[18px] (Figma 18px frame), color text-text-tertiary→text-text-primary (Figma Text/Title = white), hover colors→opacity-70. — Why: designer gave the Figma node; library edit glyph is a different shape+color. Authoritative-source. (Note: edit/history/logout DO exist at /now-gg/24/ and render — the earlier 404s were ONLY gamepad/gameplay-time at size 32, which don't exist at 32; those are now replaced by exported badges.)
2026-06-11 — ProfileSidebar.tsx (stat cards) — height h-11 (44px, Figma) → h-12 (48px) — Why: designer asked to increase a little; our drawer is wider (420) than Figma's sidebar (348) so 44px read slightly squat. Badge (h-full) scales with it. Flag at audit: this deviates +4px from Figma's 44 by designer call (valid deviation reason c).
2026-06-11 — ProfileSidebar.tsx (Subscriptions SubHeader icon) — replaced hand-drawn diamond path with the Figma "Tags/Editor" badge (node 26502:62325): hexagon + star knockout, inlined currentColor. Kept size-5 + text-text-tertiary to match the sibling "Recently played" header icon (Gate 7). — Why: designer gave the Figma node; old diamond was hand-drawn (taste-9 violation). Authoritative-source. Figma node fill is white (100%) but held at 70% for sibling consistency — flag at audit if designer wants section-header icons at full white.
2026-06-11 — ProfileSidebar.tsx (drawer panel) — removed rounded-tl-xxl (16px top-left radius); panel is now fully square — Why: designer — a full-height, edge-to-edge right drawer (inset-y-0 right-0) shouldn't carry a lone rounded corner. Generalization candidate: full-bleed edge-anchored surfaces get square corners; radius is for inset/floating surfaces.
2026-06-11 — ProfileSidebar.tsx (header) — added border-b border-line (full-width white-10 hairline) under the header — Why: header is pinned (shrink-0), body scrolls beneath it; with no boundary the scrolling profile row merges into the "Profile" title. Hairline anchors the fixed header + matches the footer's existing border-line zone separators. Designer prompted. Generalization candidate: a pinned header over a scrolling body needs a boundary (hairline or on-scroll shadow), not just spacing. OPEN: same scroll-merge applies at the body→footer edge — consider a matching top hairline on the footer.
